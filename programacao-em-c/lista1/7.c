#include <stdio.h>

int main(void)
{
    float A, B, tempo;

    printf("Escreva dois valores: ");
    scanf("%f %f", &A, &B);

    A = tempo;
    A = B;
    B = tempo;

    printf("Os numeros invertidos sao %f e %f", A, B);

    return 0;
}